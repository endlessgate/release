name: Release
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: checkout loopchain
      - uses: actions/checkout@v2
      - with:
          repository: checkout loopchain
          path: loopchain
